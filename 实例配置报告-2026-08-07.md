# Google Cloud 实例配置报告

> 隐私处理：本文件保留历史技术观测，项目/实例/账号标识和地址已替换为示例值，不可用于连接真实资源。这里的“当前”指采集当时，不代表此刻状态。

采集时间：2026-08-07 23:27 左右（Asia/Shanghai）
采集方式：SSH、实例元数据服务和本机公网端口探测；未读取密钥或访问令牌。

## 云资源信息

| 项目 | 当前值 |
|---|---|
| 项目 ID | `example-project-id` |
| 项目编号 | `REDACTED_PROJECT_NUMBER` |
| 实例 ID | `REDACTED_INSTANCE_ID` |
| 实例名称 | `example-vps` |
| 区域/可用区 | `us-west1` / `us-west1-a` |
| 机型 | `e2-micro`，E2 共享核心 |
| 当前状态 | 正在运行，未被抢占，当前无维护事件 |
| CPU 平台 | Intel Broadwell，来宾系统显示 Intel Xeon 2.20 GHz |
| 网络标签 | `http-server`、`https-server` |
| 服务账号 | `example-service-account` |

## 计算资源

| 项目 | 当前值 |
|---|---|
| 操作系统可见 CPU | 2 个 vCPU / 线程，1 核、2 线程 |
| 持续 CPU 份额 | 合计 0.25 vCPU；每个可见 vCPU 持续 12.5% CPU 时间 |
| 突发能力 | 可短时间突发；满负载突发约 30 秒，具体由令牌桶决定 |
| 内存规格 | 1 GB |
| 系统实测可用总内存 | 987,712 KiB，约 964 MiB |
| Swap | 2 GiB `/swapfile`，已启用并配置为开机自动挂载 |
| GPU / Local SSD | `e2-micro` 不支持，本实例也未发现 |
| 官方最大网络出口 | 最高 1 Gbps，实际速度取决于流量目标等因素 |

采集时内存约使用 457 MiB、可用约 507 MiB。2026-08-08 已追加 2 GiB Swap，默认
`vm.swappiness=60`；Swap 能降低突发内存压力导致进程被杀的概率，但不能替代物理内存。

## 系统与虚拟化

| 项目 | 当前值 |
|---|---|
| 发行版 | Debian GNU/Linux 13 (trixie) |
| 镜像 | `debian-13-trixie-v20260804` |
| 内核 | `6.12.100+deb13-cloud-amd64` |
| 架构 | `x86_64` |
| 虚拟化 | Google / KVM 全虚拟化 |
| 系统时区 | `Etc/UTC` |
| 本次开机时间 | `2026-08-07 14:32:04 UTC` |
| 自动安全更新定时器 | 已启用 |

## 磁盘

| 项目 | 当前值 |
|---|---|
| 启动盘设备 | `/dev/sda` |
| 云盘类别 | Google Persistent Disk，SCSI、读写模式 |
| 容量 | 30 GB |
| 根分区 | `/dev/sda1`，ext4，约 29.9 GB |
| EFI 分区 | `/dev/sda15`，vfat，124 MB |
| 当前占用 | 根分区约 2.7 GB，剩余约 26 GB，使用率 10% |

实例内部无法可靠区分该盘是 `pd-balanced`、`pd-standard` 还是 `pd-ssd`。当前服务账号
缺少读取 Compute Engine 实例详情的 OAuth scope，因此没有猜测磁盘子类型。

## 网络

| 项目 | 当前值 |
|---|---|
| VPC | `default` |
| 子网 | `default`（`us-west1`） |
| 内网 IPv4 | `10.0.0.10` |
| 公网 IPv4 | `192.0.2.10` |
| 主网卡 | `ens4` |
| MTU | 1460 |
| IPv6 | 未发现全局 IPv6 地址 |
| DNS | Google 元数据 DNS `169.254.169.254` |

公网连通性实测：

| 端口 | 状态 | 用途 |
|---|---|---|
| 22/TCP | 可达 | SSH |
| 80/TCP | 不可达 | 当前没有 Web 服务 |
| 443/TCP | 不可达 | 当前没有 HTTPS 服务 |
| 20201/TCP | **公网可达** | Ops Agent 自监控指标 |
| 20202/TCP | **公网可达** | Ops Agent 自监控指标 |

`20201` 和 `20202` 按 Google 文档只需在虚拟机内部可用，不需要对公网开放。当前公网
可建立连接，建议检查 VPC 防火墙入站规则并限制这两个端口；本次只检查，没有修改。

## Google 来宾组件与服务

| 服务 | 状态 |
|---|---|
| SSH 服务 | 运行中 |
| Google Guest Agent Plugin Manager | 运行中 |
| Google Guest Compat Manager | 运行中 |
| Google OS Config Agent | 运行中 |
| Google Cloud Ops Agent | 运行中 |
| Ops Agent 日志组件 | 运行中 |
| Ops Agent 指标组件 | 运行中 |
| Ops Agent 版本 | `2.70.0~debian13` |
| Guest Agent 版本 | `1:20260715.00-g1` |

实例服务账号的访问 scope 仅包括 Cloud Storage 只读，以及日志、监控、Trace 等写入
权限。它不能调用 Compute API 读取实例控制面详情，因此删除保护、Shielded VM 开关、
调度策略、磁盘精确子类型和公网 IP 是否为静态地址，本报告无法从实例内部确认。

## 官方规格参考

- E2 机型：<https://docs.cloud.google.com/compute/docs/general-purpose-machines>
- Persistent Disk：<https://docs.cloud.google.com/compute/docs/disks/persistent-disks>
- Ops Agent：<https://docs.cloud.google.com/stackdriver/docs/solutions/agents/ops-agent>
