# Google Cloud 美国单实例代理整体方案

> 隐私处理：域名、SSH 别名、本机用户路径等已改为示例。本文保留架构和历史验收，不提供真实连接信息；新部署以 Codex Skill 为入口。

整理日期：2026-08-31
适用场景：中国家庭网络通过一台 Google Cloud 美国虚拟机访问境外网络，并在 Clash Verge 中通过订阅管理节点。

2026-08-31 更新：Trojan 已从 TCP 443 迁到 TCP 10443，家庭电脑连续 100 次代理请求全部成功，主备订阅已同步。
Hysteria UDP 443、REALITY TCP 2053 与原凭据未变，两个原节点迁移前后均实测通过。
只做最小端口修改，没有升级或重装服务；若后续 Trojan 不稳定，优先保留原节点，不增加复杂方案。晚高峰与 ChatGPT 浏览器体验尚未完成验收。

> 本文是可交付、可长期保存的脱敏方案。UUID、REALITY 私钥、Hysteria2/Trojan 密码和订阅令牌均不写入文件。

## 一、方案结论

最终采用一台 Google Cloud 美国实例、三个代理协议、一个 Cloudflare 聚合主订阅和一个服务器显式备用订阅：

- 不创建日本或新加坡实例。
- 不使用 Docker 承载代理核心，使用原生 systemd，减少中间层和内存开销。
- Hysteria2 使用 UDP 443，作为普通境外流量的首选节点。
- Xray Trojan + TLS 使用 TCP 10443，作为标准 TLS TCP 兼容线路；未认证访问只回落到本机静态 Nginx。
- Xray VLESS + REALITY + XTLS Vision 使用 TCP 2053，作为 UDP 受限时的备用节点。
- Cloudflare Worker 只提供订阅文件，不转发代理流量。
- Clash 通过自动策略组在三个协议间进行健康检查和故障切换。

一台美国 VM 只能产生美国出口 IP。修改节点名称、SNI、接入 Cloudflare 或 WARP，都不能把最终出口可靠地变成日本或新加坡。订阅服务里出现多个国家，是因为服务商实际部署或租用了多个国家的服务器。

美国属于 ChatGPT 支持地区，因此日常使用 ChatGPT 不必强行增加亚洲实例。如果以后业务必须显示日本或新加坡出口，只能增加相应地区的真实服务器，或者购买对应地区的上游代理服务。

## 二、现有基础资源

| 项目 | 当前配置 |
| --- | --- |
| 云平台 | Google Cloud Compute Engine |
| 实例 | `example-vps` |
| 地区 | `us-west1-a`，美国俄勒冈 |
| 机型 | `e2-micro`，共享核心，1 GB 内存 |
| 系统 | Debian GNU/Linux 13，x86_64 |
| 启动盘 | 30 GB Persistent Disk |
| 公网地址 | 保存在本机 SSH 配置与 DNS 中，不写入仓库文档 |
| 节点域名 | `us1.example.com` |
| 订阅域名 | `sub.example.com` |
| Docker | 保留，但当前没有运行容器 |

服务器增加了 2 GiB Swap，用于降低小内存机器在更新软件或流量突发时被 OOM 杀死的风险。Swap 不能代替物理内存，因此不建议在这台机器上继续叠加面板、数据库或其他常驻重服务。

## 三、总体架构

```text
中国家庭电脑 / Clash Verge
        │
        ├── 获取订阅 ──> sub.example.com
        │                 Cloudflare Worker
        │                 └── 私有 KV：clash-config、v2ray-config
        │
        ├── 备用订阅 ──> us1.example.com:8443
        │                 Nginx + 独立订阅令牌
        │
        ├── TCP 10443 ─> Xray Trojan + TLS
        │
        ├── TCP 2053 ──> Xray VLESS + REALITY + Vision
        │
        └── UDP 443 ───> Hysteria2
                          │
                          └── 美国公网出口
```

Cloudflare 不在代理数据链路中。访问网页、视频和 ChatGPT 的实际流量直接在家庭网络与 Google Cloud VM 之间传输，因此线路延迟、晚高峰表现和流量费用仍由 Google Cloud 网络决定。

## 四、服务部署

### 4.1 Xray REALITY

- 软件：Xray `v26.3.27`。
- 协议：VLESS。
- 传输：原始 TCP。
- 安全层：REALITY。
- Flow：`xtls-rprx-vision`。
- 监听：TCP 2053。
- REALITY TLS target：`127.0.0.1:8443` 的本机 Nginx TLS 站点。
- 运行方式：独立 systemd 服务 `xray.service`。

REALITY 节点用于 TCP 兼容性场景，尤其适合 UDP 被运营商、路由器或公共 Wi-Fi 限制时作为备用。

### 4.2 Trojan TLS

- 软件：Xray `v26.3.27`。
- 协议：Trojan。
- 传输与安全层：TCP + TLS。
- 监听：TCP 10443，已通过家庭电脑短时代理请求验收。
- TLS serverName：节点域名；Xray 使用权限收紧的证书副本。
- 未认证 fallback：`127.0.0.1:8080` 的本机静态 Nginx，不会转发到外部网站。
- 运行方式：与 REALITY 共用 `xray.service`。

Trojan 为 UDP 不可用、REALITY 客户端兼容性不足或 TCP 2053 受限的环境提供标准 TLS TCP 备用。

### 4.3 Hysteria2

- 软件：Hysteria2 `v2.12.1`。
- 传输：QUIC/UDP。
- 监听：UDP 443。
- TLS 证书：Let's Encrypt，为 `us1.example.com` 签发。
- masquerade：代理到 `127.0.0.1:8080` 的本机静态 Nginx。
- 运行方式：独立 systemd 服务 `hysteria-server.service`。

REALITY 与 Hysteria2 使用不同端口和传输协议。Hysteria2 更适合高延迟、存在一定丢包的移动网络环境，因此作为普通境外流量的默认首选。REALITY 曾使用 TCP 443，但因未认证 fallback 遭遇大规模连接和带宽滥用，于 2026-08-25 迁移到 TCP 2053，并把 fallback 收敛到本机 Nginx。

### 4.4 Nginx 与证书

- TCP 80：Let's Encrypt HTTP-01 验证，并跳转 HTTPS。
- TCP 8443：提供备用 Clash YAML 与 URI/Base64 显式订阅。
- 本机 TCP 8080：仅供 Trojan fallback 与 Hysteria2 masquerade 使用；REALITY TLS target 为本机 TCP 8443 的 Nginx TLS 站点。
- Certbot timer：自动检查并续期证书。
- 续期 deploy hook：同步 Xray 与 Hysteria2 的证书副本，重启 Xray 与 Hysteria2 并重新载入 Nginx。

证书自动续期 dry-run 已验证成功。

### 4.5 系统网络优化

- TCP 拥塞控制：BBR。
- 默认队列：`fq`。
- TCP MTU probing：开启。
- VM 网卡 MTU：1460，由 Google Cloud 网络环境决定。
- Xray、Hysteria2 和相关服务启用自动重启。
- 安装 `vnstat` 统计长期流量。

## 五、域名和订阅设计

| 地址 | 用途 | Cloudflare 代理状态 |
| --- | --- | --- |
| `us1.example.com:2053/TCP` | VLESS REALITY | DNS-only |
| `us1.example.com:443/UDP` | Hysteria2 | DNS-only |
| `us1.example.com:10443/TCP` | Trojan TLS | DNS-only |
| `us1.example.com:80/TCP` | ACME 验证与跳转 | DNS-only |
| `us1.example.com:8443/TCP` | 备用 Clash YAML 与 URI/Base64 显式订阅 | DNS-only |
| `sub.example.com` | Worker 主订阅 | Cloudflare Worker 自定义域名 |

`test.example.com` 没有修改。

### 5.1 主订阅

主订阅由 Cloudflare Worker 提供统一入口，并保留两条显式格式入口：

```text
https://sub.example.com/<64位随机令牌>/subscription
```

Worker 的安全行为：

- 只接受 GET 和 HEAD。
- 只接受完全匹配的令牌路径。
- 其他路径统一返回 404。
- `subscription` 可用 `target=clash` 或 `target=v2ray` 显式选择格式；未指定时按受控客户端 User-Agent 协商，未知客户端默认 URI/Base64。
- `clash.yaml` 与 `v2ray.txt` 是不依赖 User-Agent 的显式入口。
- 从私有 Workers KV 的 `clash-config`（Clash YAML）与 `v2ray-config`（通用 URI/Base64）两份配置读取内容。
- 返回 `Cache-Control: no-store`，防止订阅内容被 CDN 或浏览器缓存。
- 使用 `nosniff`、`DENY` 和 `no-referrer` 等响应头降低意外泄露风险。

当前个人使用量远低于 Cloudflare Workers Free 计划的每天 100,000 请求与 KV 每天 100,000 次读取额度，因此预计费用为 0 美元。

### 5.2 备用订阅

备用订阅由服务器 Nginx 在 8443 端口提供两种显式格式：

```text
https://us1.example.com:8443/<另一枚独立随机令牌>/clash.yaml
https://us1.example.com:8443/<另一枚独立随机令牌>/v2ray.txt
```

主、备用订阅使用两个不同令牌。Cloudflare Worker 不可用时，可以在 Clash 中临时使用备用地址。

需要在服务器上查看实际订阅地址时执行：

```bash
sudo cat /etc/proxy-stack/subscription-urls.txt
```

不要截图、公开或转发实际订阅地址。得到订阅地址的人相当于获得全部三条代理线路的使用权。

## 六、Clash 行为

订阅已经导入 Windows Clash Verge，自动更新已开启。导入时保留了用户原来的当前配置，没有强制切换，以避免突然中断现有连接。

### 6.1 节点与策略组

| 名称 | 类型与行为 |
| --- | --- |
| `US-Hysteria2` | Hysteria2 节点，UDP 443 |
| `US-Trojan` | Trojan TLS 节点，TCP 10443，已验收与同步订阅 |
| `US-Reality` | VLESS REALITY 节点，TCP 2053 |
| `US-Auto` | 依次尝试 Hysteria2、Trojan、REALITY |
| `Proxy-Select` | 默认使用 `US-Auto`，也允许手动指定协议 |
| `ChatGPT` | 固定使用 `US-Auto` |

普通境外流量与 ChatGPT 都使用 `US-Auto`，优先 Hysteria2，再依次尝试 Trojan 和 REALITY；这样策略不把 ChatGPT 锁定到单一 TCP 协议。

### 6.2 分流与 DNS

- OpenAI、ChatGPT、登录、静态资源、文件、WebSocket、Codex 和 Voice 相关域名固定进入 `ChatGPT` 策略组。
- 中国大陆域名与 IP 直连。
- 私网和本机域名直连。
- 其他境外流量进入 `Proxy-Select`。
- DNS 使用 Fake-IP 模式。
- 国内域名由国内 DoH 直连解析。
- 境外 DNS 查询跟随代理，降低 DNS 污染风险。

节点不会虚假命名为日本或新加坡，避免日后误判实际出口国家。

## 七、安全设计

### 7.1 主机防火墙

nftables 入站策略为默认丢弃，只允许：

- TCP 22：SSH，并带新连接速率限制。
- TCP 80：ACME 和跳转。
- TCP 10443：Trojan TLS，带全局新连接速率保护。
- TCP 2053：Xray REALITY，带全局新连接速率保护。
- TCP 8443：备用订阅。
- UDP 443：Hysteria2。
- 必要的 ICMP、DHCP 和已建立连接。

TCP 443 新连接已不再放行，由主机默认 drop 拒绝，UDP 443 不变；TCP 10443 已通过家庭网络真实代理请求验收。旧的端口可达记录仅代表各自记录时的状态。

### 7.2 SSH 加固

- 只允许密钥认证。
- 禁止密码认证。
- 禁止 root 直接登录。
- 最大认证尝试次数为 3。
- 登录宽限时间为 30 秒。
- 22 端口按来源 IP 限制为每分钟 30 个、突发 20 个新连接。

### 7.3 代理出站 ACL

Xray 和 Hysteria2 禁止代理客户端访问：

- 回环地址。
- RFC1918 私网地址。
- 链路本地地址。
- GCP Metadata 地址。
- TCP 25，降低邮件滥用风险。

### 7.4 凭据保护

- 节点 UUID、REALITY 私钥、Hysteria2/Trojan 密码和两个订阅令牌分别随机生成。
- 服务端主凭据位于 `/etc/proxy-stack/secrets.env`。
- 凭据文件权限为 `0600 root:root`。
- 仓库与工作总结均未保存真实节点凭据。
- 临时测速核心、测试 YAML、缓存和并发测试文件已在验收后删除。
- Xray 文件描述符上限为 8192；TCP 10443 和 TCP 2053 均有每秒 60 个、突发 120 个的新连接保护。
- 未认证 Trojan 与 REALITY 连接只回落到本机 Nginx，不再产生到外部伪装网站的中继流量。

### 7.5 Google 服务组件

无权限且曾暴露 20201、20202 的 Google Ops Agent 已停止并 mask。Google Guest Agent 和 OSConfig 保留，不影响 Google Cloud 的基础实例管理。

## 八、费用说明

### 8.1 费用组成

| 项目 | 预计月费 |
| --- | ---: |
| `e2-micro` VM | 符合 us-west1 免费层时为 `$0` |
| 30 GB `pd-standard` | 免费层内为 `$0` |
| 若启动盘实际为 30 GB `pd-balanced` | 约 `$3` |
| 在用公网 IPv4 | 约 `$0.005/小时`，即 `$3.65/月` |
| Cloudflare Worker + KV | 当前个人用量下为 `$0` |
| Let's Encrypt、Xray、Hysteria2 | `$0` |
| 美国 VM 向中国大陆发出的流量 | 约 `$0.23/GiB` |

当前实例内部没有 Compute 控制面读取权限，因此无法可靠确认磁盘究竟是 `pd-standard` 还是 `pd-balanced`，也无法读取实时 Billing 账单。保守计算固定成本约为：

```text
$3.65～$6.65/月
```

主要费用公式：

```text
预计月费 ≈ $3.65～$6.65 + 中国方向下载流量 × $0.23/GiB
```

2026-08-25 处置连接风暴时，`vnstat` 累计约 144.6 GB 出站。若这些流量全部按中国方向
Premium Tier 标价计算，理论上约为 33 美元；这不是 Billing 实际账单值，最终应以 Google Cloud
Billing 报表为准。试用期内通常由 300 美元额度抵扣。

### 8.2 使用量示例

| 每月代理下载量 | 预计月费 |
| ---: | ---: |
| 10 GiB | `$5.95～$8.95` |
| 50 GiB | `$15.15～$18.15` |
| 100 GiB | `$26.65～$29.65` |
| 300 GiB | `$72.65～$75.65` |
| 500 GiB | `$118.65～$121.65` |

ChatGPT、GitHub 和普通网页通常流量较小；YouTube、高清视频、系统镜像和大文件下载会迅速增加费用。每增加 100 GiB 中国方向出站流量，大约增加 23 美元。

### 8.3 300 美元试用额度

Google Cloud 免费试用提供 300 美元额度，有效期 90 天。试用期间不会直接从银行卡扣费，使用成本从额度中抵扣：

- 如果未升级为付费账号，90 天到期或 300 美元耗尽后，项目资源会停止，不会自动开始扣款。
- 如果升级为付费账号，剩余额度仍可用到原 90 天期限；超过剩余额度、免费层或额度有效期后才会实际扣费。
- 以每月 100 GiB 计算，三个月大约消耗 80～90 美元。
- 以每月 300 GiB 计算，三个月大约消耗 218～227 美元。
- 为避免三个月内用完 300 美元，平均代理下载量最好控制在约 13 GiB/天以内。

建议在 Google Cloud 控制台设置项目范围的每月 100 美元预算告警，阈值设为 50%、90%、100%。预算告警只发送通知，不是硬性消费上限。当前 VM 服务账号没有 Billing 权限，因此该项需要使用项目所有者账号在控制台手工完成。

## 九、验收记录

2026-08-31：Trojan TCP 10443 家庭电脑连续 100/100 请求通过；Hysteria2、REALITY 迁移后各 6/6 请求通过。三个节点出口均为美国，主备订阅完全一致，订阅地址与原节点凭据未变。详见[迁移日志](./worklog/2026-08-31-Trojan最小迁移与原节点保护.md)。

以下为 2026-08-30 历史状态：

- Xray、Hysteria2、Nginx、nftables 和 certbot timer 均保持 active；Xray 同时监听 Trojan TCP 443 与 REALITY TCP 2053，Hysteria2 监听 UDP 443。
- Trojan 通过临时本机 SOCKS 客户端完成真实 TLS 代理请求并获得 HTTP 204；出口国家为美国。Hysteria2 和 REALITY 服务保持 active。
- OpenAI API 可以建立连接；未提供 API Key 时返回预期的 `401`。
- 以下并发结果为 2026-08-25 的历史验收：当时两个协议分别完成 50/50 并发代理请求。
- `US-Auto` 的当前顺序为 Hysteria2、Trojan、REALITY；ChatGPT 使用该自动组。
- Worker 统一入口的两种 `target`、典型 Clash/V2Ray 客户端识别、主显式两个入口和 8443 备用两个入口均通过验证；同格式内容哈希一致。
- Worker 错误令牌路径返回 404，正确路径返回 200 且禁止缓存；URI/Base64 解码后恰为三条 URI。
- Let's Encrypt 续期 dry-run 成功。
- Xray 和 Nginx 配置检查成功。
- Ops Agent 已停止，20201 和 20202 不再监听。
- Docker 保留且运行容器数为 0。
- 以下两项为 2026-08-25 的历史验收：当时两个协议分别完成 10/10 请求，REALITY fallback 收敛到本机后又完成 5/5 合法客户端请求。
- 连续 5 次 SSH 登录全部成功；实例负载由故障时最高 22.90 回落到接近 0。

尚需用户在中国移动家庭网络完成真实浏览器体验验收：

1. Google、GitHub 和 YouTube 基本访问。
2. ChatGPT 登录、连续长回复和新会话。
3. WebSocket、文件上传、Codex 和 Voice。
4. 晚高峰连续使用不少于 30 分钟。
5. 检查国内网站是否直连，境外 DNS 是否正常。

命令行访问 ChatGPT 返回 Cloudflare challenge 不能单独说明 IP 被封。如果真实浏览器在新会话、清理 Cookie 后仍持续出现“异常活动”，再为同一台 VM 更换临时公网 IP，并重新验收后决定是否固定地址。

## 十、日常维护

### 10.1 服务状态

```bash
systemctl status xray hysteria-server nginx proxy-firewall vnstat certbot.timer
```

### 10.2 查看日志

```bash
sudo journalctl -u xray --since "30 minutes ago" --no-pager
sudo journalctl -u hysteria-server --since "30 minutes ago" --no-pager
sudo journalctl -u nginx --since "30 minutes ago" --no-pager
```

### 10.3 流量与资源

```bash
vnstat
free -h
df -h /
```

### 10.4 配置与证书检查

```bash
sudo /usr/local/bin/xray run -test -config /etc/xray/config.json
sudo nginx -t
sudo certbot renew --dry-run --no-random-sleep-on-renew
sudo nft list chain inet proxy_guard input
```

### 10.5 重启服务

```bash
sudo systemctl restart xray
sudo systemctl restart hysteria-server
sudo systemctl reload nginx
```

如果 Hysteria2 失效，Clash 会按 `US-Auto` 顺序尝试 Trojan、再尝试 REALITY，不必立刻修改订阅。

## 十一、更新与恢复

可重复执行的服务器脚本：

```text
deploy/server-bootstrap.sh
```

重新部署：

```powershell
scp .\deploy\server-bootstrap.sh my-proxy-vps:/tmp/proxy-bootstrap.sh
ssh my-proxy-vps "sudo bash /tmp/proxy-bootstrap.sh"
```

脚本会：

- 备份现有配置到 `/root/proxy-stack-backup-<UTC时间>`。
- 校验 Xray 和 Hysteria2 下载文件，并生成 Trojan 配置与受限证书副本。
- 若凭据文件已经存在，则保留原有随机凭据。
- 重建服务、ACL、防火墙、订阅文件和系统优化。

如果订阅内容变化，需要把 `/var/lib/proxy-subscription/clash.yaml` 同步到 Cloudflare KV 的 `clash-config`，并把 `/var/lib/proxy-subscription/v2ray.txt` 同步到 `v2ray-config`；备用订阅会直接读取服务器的新文件。部署前先比较 Worker 与备用入口的同格式哈希；服务器备份中的旧订阅文件可作为 KV 恢复源，但 Cloudflare KV、secret 和 Worker 不会被部署脚本自动备份。覆盖 KV 前，应以锁定 Wrangler 将当前两个 key 保存到 Git 忽略、当前用户独占的本地备份目录；Worker 通过 Git 上一已知版本重新部署或在 Cloudflare 控制台回退版本。

如果公网 IP 变化，需要同步更新：

1. Cloudflare 中 `us1.example.com` 的 DNS-only A 记录。
2. 本机 `C:\Users\vps-user\.ssh\config` 中 `my-proxy-vps` 的 `HostName`。
3. DNS 生效后重新验证证书、三个协议、聚合订阅和 ChatGPT。

如果恢复到全新实例，部署脚本会生成全新凭据，必须同步更新 Worker secret、KV 内容和 Clash 订阅。绝不能把 `/etc/proxy-stack/secrets.env` 上传到代码仓库或发送给他人。

## 十二、明确边界与后续选择

- 本方案只有美国出口，不提供真正的日本或新加坡 IP。
- Cloudflare Worker、CDN 和 WARP 不用于伪造日本出口，也不能改善中国到美国 VM 的主要物理路径。
- 不推荐“国内 → 美国 VM → 日本商业 VPN”的链式方案：延迟更高、故障点更多，而且仍需额外购买日本上游。
- 如果以后必须要日本或新加坡出口，应直接增加该地区的真实服务器，或者选择信誉可靠且明确提供当地出口的服务商。
- 目前不建议升级机型。单人轻量使用时 `e2-micro` 足够；只有在 CPU 长期打满、内存频繁使用 Swap 或多用户并发时，再考虑 `e2-small` 或更高规格。
- 大流量视频与下载是费用风险的核心，应以 `vnstat` 和 Google Cloud Billing 报表共同监控。

## 十三、相关文件

- `deploy/server-bootstrap.sh`：服务器可重复部署脚本。
- `cloudflare-worker/src/index.js`：Worker 订阅接口源码。
- `cloudflare-worker/wrangler.jsonc`：Worker、KV 和自定义域名配置。
- `docs/美国单实例代理运维.md`：更精简的日常运维手册。
- `worklog/2026-08-12-美国单实例代理节点.md`：本次部署工作总结。

## 十四、官方参考

- Google Cloud 免费层与 300 美元试用：<https://docs.cloud.google.com/free/docs/free-cloud-features>
- Google Cloud 网络价格：<https://cloud.google.com/vpc/network-pricing>
- Google Cloud 磁盘价格：<https://cloud.google.com/compute/disks-image-pricing>
- Google Cloud 预算告警：<https://docs.cloud.google.com/billing/docs/how-to/budgets>
- Cloudflare Workers 价格：<https://developers.cloudflare.com/workers/platform/pricing/>
- Cloudflare Workers KV 价格：<https://developers.cloudflare.com/kv/platform/pricing/>
- Xray 发布页：<https://github.com/XTLS/Xray-core/releases>
- Hysteria2 文档：<https://v2.hysteria.network/docs/>
- ChatGPT 支持地区：<https://help.openai.com/en/articles/7947663-chatgpt-supported-countries-and-territories>
- ChatGPT 网络域名建议：<https://help.openai.com/en/articles/9247338-network-recommendations-for-chatgpt-errors-on-web-and-apps>
